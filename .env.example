# 1Click Partners Portal JWT (Bearer). Required for real (non-dry) quotes, submitDepositTx, and status.
# Obtain at https://partners.near-intents.org. Copy this file to .env and fill in the value.
# Used by `npm run test:live:auth` (see README).
ONE_CLICK_JWT=
